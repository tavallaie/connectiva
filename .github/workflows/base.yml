name: Base Setup

on:
  workflow_call:
    inputs:
      python-version:
        description: 'The Python version to use'
        required: true
        type: string

jobs:
  setup:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ inputs.python-version }}

      - name: Install Poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry

      - name: Install dependencies
        run: poetry install
      - name: List Docker Directory
        run: |
          echo "Listing contents of the docker directory:"
          ls -la docker/
        shell: bash